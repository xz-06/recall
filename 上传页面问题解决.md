# 🔧 上传页面问题解决

## 🔍 问题分析

你访问的是 `your-backend.railway.app/upload`，这个地址是占位符，不存在。

**原因**：
- `docs/js/config.js` 中配置的是占位符地址
- 上传页面需要部署到Vercel

## ✅ 解决方案

### 已修复的配置

1. **更新了 `docs/js/config.js`**
   - 改为使用当前域名（Vercel自动）
   - 不再使用Railway占位符地址

2. **更新了 `vercel.json`**
   - 添加了重写规则，`/upload` 自动指向 `upload.html`

3. **复制了 `upload.html` 到 `site/` 目录**
   - 确保上传页面被部署

### 下一步操作

#### 步骤1：重新构建并部署

```bash
# 重新构建（确保upload.html被复制）
npm run mkdocs:build

# 提交更改
git add docs/js/config.js docs/index.md vercel.json site/upload.html
git commit -m "修复上传页面链接配置"
git push origin main
```

#### 步骤2：验证配置

部署完成后：

1. **访问上传页面**：
   ```
   https://recall-self.vercel.app/upload
   ```

2. **应该能看到上传表单**

3. **测试上传**：
   - 填写标题、日期、内容
   - 上传图片（可选）
   - 点击保存

## 🎯 正确的访问地址

- **网站首页**：https://recall-self.vercel.app
- **上传页面**：https://recall-self.vercel.app/upload
- **API端点**：https://recall-self.vercel.app/api/upload

**不要访问**：`your-backend.railway.app`（这是占位符，不存在）

## 🔍 如果还是404

### 检查1：确认upload.html已部署

访问：https://recall-self.vercel.app/upload.html

如果能访问，说明文件已部署，只是路由配置问题。

### 检查2：查看Vercel部署日志

1. 访问Vercel Dashboard
2. 查看最新部署的日志
3. 确认 `upload.html` 是否在 `site/` 目录中

### 检查3：手动添加重写规则

如果自动重写不工作，在Vercel Dashboard中：
1. Settings → **Rewrites**
2. 添加：
   ```
   Source: /upload
   Destination: /upload.html
   ```

## 📝 配置说明

### Vercel Functions工作流程

1. **前端**：Vercel部署（静态站点）
2. **上传页面**：`/upload` → `upload.html`
3. **API**：`/api/upload` → Vercel Functions
4. **保存**：通过GitHub API保存到GitHub仓库

### 环境变量已配置 ✅

你的 `GITHUB_TOKEN` 已配置，上传功能应该可以正常工作。

---

💕 重新部署后，访问 `https://recall-self.vercel.app/upload` 应该就能正常使用了！

