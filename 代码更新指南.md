# 📝 代码更新指南

当你已经配置好GitHub部署后，更新代码非常简单！每次推送代码到GitHub，GitHub Actions会自动构建和部署。

## 🚀 更新流程

### 1. 修改代码

在本地编辑器中修改任何文件：
- 修改 `docs/` 目录下的Markdown文件
- 修改 `server/` 目录下的服务器代码
- 修改 `upload.html` 上传页面
- 修改 `mkdocs.yml` 配置
- 等等...

### 2. 查看修改

```bash
# 查看哪些文件被修改了
git status

# 查看具体的修改内容
git diff
```

### 3. 添加修改到暂存区

```bash
# 添加所有修改的文件
git add .

# 或者只添加特定文件
git add docs/index.md
git add server/index.js
```

### 4. 提交修改

```bash
# 提交修改，并写清楚修改了什么
git commit -m "修改首页，添加上传链接"

# 提交信息示例：
# git commit -m "更新回忆内容"
# git commit -m "修复上传功能bug"
# git commit -m "添加新的回忆"
```

### 5. 推送到GitHub

```bash
# 推送到GitHub
git push origin main
```

### 6. 自动部署

推送完成后，GitHub Actions会自动：
1. ✅ 检测到代码更新
2. ✅ 安装Python依赖
3. ✅ 构建MkDocs站点
4. ✅ 部署到GitHub Pages

等待2-5分钟，网站就会自动更新！

## 📋 完整示例

假设你要修改首页内容：

```bash
# 1. 修改文件（在编辑器中编辑 docs/index.md）

# 2. 查看修改
git status

# 3. 添加修改
git add docs/index.md

# 4. 提交
git commit -m "更新首页欢迎文字"

# 5. 推送
git push origin main

# 6. 等待GitHub Actions完成部署（约2-5分钟）
```

## 🔍 查看部署状态

### 方法1：GitHub网页

1. 访问你的仓库：`https://github.com/xz-06/recall`
2. 点击 **"Actions"** 标签
3. 查看最新的工作流运行状态
4. 绿色✅ = 部署成功，红色❌ = 部署失败

### 方法2：GitHub CLI

```bash
gh run list
gh run watch
```

## 📝 常用场景

### 场景1：添加新回忆

如果你通过上传页面添加了回忆，需要提交新文件：

```bash
# 1. 查看新文件
git status

# 2. 添加新的回忆文件
git add docs/memories/*.md
git add docs/images/*.jpg

# 3. 提交
git commit -m "添加新回忆：2024-12-17"

# 4. 推送
git push origin main
```

### 场景2：修改上传页面

```bash
# 1. 修改 upload.html

# 2. 添加、提交、推送
git add upload.html
git commit -m "优化上传页面样式"
git push origin main
```

### 场景3：修改服务器代码

```bash
# 1. 修改 server/index.js 或 server/routes/upload.js

# 2. 添加、提交、推送
git add server/
git commit -m "修复上传API bug"
git push origin main
```

**注意**：服务器代码的修改需要重新部署后端服务器（如果使用Railway/Render等），GitHub Pages只部署静态站点。

### 场景4：修改MkDocs配置

```bash
# 1. 修改 mkdocs.yml

# 2. 添加、提交、推送
git add mkdocs.yml
git commit -m "更新MkDocs主题配置"
git push origin main
```

## ⚠️ 注意事项

### 1. 不要提交敏感信息

确保以下文件在 `.gitignore` 中：
- `.env` - 环境变量
- `node_modules/` - 依赖包
- `site/` - 构建输出（会自动生成）
- `uploads/` - 临时上传文件

### 2. 提交前测试

建议在本地先测试：

```bash
# 构建站点
npm run mkdocs:build

# 启动开发服务器测试
npm run dev
```

### 3. 提交信息要清晰

好的提交信息示例：
- ✅ `"添加新回忆：2024-12-17"`
- ✅ `"修复上传页面返回链接"`
- ✅ `"更新首页内容"`
- ❌ `"修改"`
- ❌ `"更新"`

### 4. 图片文件大小

GitHub对单个文件有100MB限制，建议：
- 图片压缩后再上传
- 大文件使用Git LFS

## 🔄 回退修改

如果推送后发现有问题，可以回退：

```bash
# 查看提交历史
git log

# 回退到上一个提交
git revert HEAD
git push origin main

# 或者回退到特定提交
git revert <commit-hash>
git push origin main
```

## 📚 更多Git命令

```bash
# 查看提交历史
git log --oneline

# 查看特定文件的修改历史
git log docs/index.md

# 撤销未提交的修改
git checkout -- <file>

# 撤销已添加到暂存区的文件
git reset HEAD <file>

# 创建新分支（用于实验性功能）
git checkout -b feature/new-feature
git push origin feature/new-feature
```

## 🆘 常见问题

### Q: 推送后网站没有更新？

A: 
1. 检查GitHub Actions是否成功运行
2. 等待几分钟（部署需要时间）
3. 清除浏览器缓存后刷新
4. 检查GitHub Pages设置是否正确

### Q: 如何只更新特定文件？

A: 
```bash
git add <specific-file>
git commit -m "更新特定文件"
git push origin main
```

### Q: 如何查看部署日志？

A: 
1. 访问GitHub仓库
2. 点击"Actions"标签
3. 点击最新的工作流运行
4. 查看构建日志

### Q: 可以同时修改多个文件吗？

A: 可以！一次性添加所有修改：
```bash
git add .
git commit -m "批量更新：修改多个文件"
git push origin main
```

---

💕 记住：**修改 → 提交 → 推送 → 自动部署**，就这么简单！

