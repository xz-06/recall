# 📦 安装说明

## Windows安装

### 方法1：使用安装脚本（推荐）

双击运行 `setup.bat` 文件。

如果遇到权限错误，请：
1. **右键点击 `setup.bat`**
2. **选择"以管理员身份运行"**

### 方法2：手动安装

#### 步骤1：安装Node.js依赖

```bash
npm install
```

#### 步骤2：安装Python依赖

**如果遇到权限错误，使用以下命令：**

```bash
# 方法1：用户模式安装（推荐）
pip install --user -r requirements.txt

# 方法2：如果方法1失败，以管理员身份运行PowerShell/CMD
pip install -r requirements.txt

# 方法3：使用pip3
pip3 install --user -r requirements.txt
```

### 常见问题

#### 问题1：权限错误（WinError 5）

**错误信息**：
```
ERROR: Could not install packages due to an EnvironmentError: [WinError 5] 拒绝访问
```

**解决方法**：
1. 使用 `--user` 选项：
   ```bash
   pip install --user -r requirements.txt
   ```

2. 以管理员身份运行：
   - 右键点击 PowerShell 或 CMD
   - 选择"以管理员身份运行"
   - 然后执行安装命令

3. 使用虚拟环境（推荐）：
   ```bash
   # 创建虚拟环境
   python -m venv venv
   
   # 激活虚拟环境
   venv\Scripts\activate
   
   # 安装依赖
   pip install -r requirements.txt
   ```

#### 问题2：pip命令未找到

**解决方法**：
1. 确保Python已正确安装
2. 检查Python是否添加到PATH环境变量
3. 尝试使用 `python -m pip` 代替 `pip`

#### 问题3：依赖版本冲突

**解决方法**：
```bash
# 升级pip
python -m pip install --upgrade pip

# 清理缓存后重新安装
pip cache purge
pip install --user -r requirements.txt
```

## Mac/Linux安装

### 使用虚拟环境（推荐）

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 直接安装

```bash
# 安装Node.js依赖
npm install

# 安装Python依赖
pip3 install -r requirements.txt
```

## 验证安装

安装完成后，验证是否成功：

```bash
# 检查Node.js
node --version
npm --version

# 检查Python
python --version
pip --version

# 检查MkDocs
mkdocs --version
```

如果所有命令都能正常执行，说明安装成功！

## 下一步

安装完成后，运行：

```bash
# 构建站点
npm run mkdocs:build

# 启动开发服务器
npm run dev
```

---

💕 如有问题，请查看错误信息并参考上述解决方法！

